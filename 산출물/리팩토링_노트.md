# 리팩토링 노트 (2026-04-22)

시니어 엔지니어 코드 리뷰에서 발견된 22건의 이슈를 수정 완료.

---

## CRITICAL (6건)

### 1. `/api/reviews/unreplied` 라우트 충돌 해결
- **파일**: `backend/app/routers/shop.py`
- **수정**: `unreplied` 라우트를 `{review_id}` 라우트보다 위로 이동, 하단의 중복 정의 제거

### 2. N+1 쿼리 (히스토리) 해결
- **파일**: `backend/app/routers/agent.py`
- **수정**: `get_history()`에서 for loop 내 개별 Review 조회를 `joinedload(Reply.review)` + `outerjoin`으로 변경하여 단일 쿼리로 처리

### 3. `lazy="joined"` 과잉 로딩 해결
- **파일**: `backend/app/models.py`
- **수정**: Product.reviews, Review.replies, Reply.history 관계의 `lazy="joined"`를 `lazy="select"`로 변경

### 4. `innerHTML` XSS 벡터 제거
- **파일**: `frontend/src/components/ProductCard.tsx`, `frontend/src/app/shop/products/[id]/page.tsx`
- **수정**: `onError` 핸들러에서 `innerHTML` DOM 조작을 제거하고, `ProductImage` 공통 컴포넌트(React state 기반 이미지 에러 관리)로 교체

### 5. 동기 LLM 클라이언트 이벤트 루프 블로킹 해결
- **파일**: `backend/app/llm_service.py`
- **수정**: `classify_sentiment()`, `generate_reply()` 내 동기 `c.messages.create()` 호출을 `await asyncio.to_thread()`로 감싸서 스레드풀에서 실행

### 6. 에러 silent 무시 해결
- **파일**: `frontend/src/app/shop/page.tsx`, `frontend/src/components/FABMiniPanel.tsx`, `frontend/src/app/shop/products/[id]/page.tsx`, `frontend/src/app/dashboard/history/page.tsx`
- **수정**: error state 추가, 에러 발생 시 "데이터를 불러올 수 없습니다. 다시 시도해주세요." 메시지 + 재시도 버튼 표시

---

## HIGH (7건)

### 7. 순차 LLM 호출 병렬화
- **파일**: `backend/app/routers/agent.py`
- **수정**: `generate_batch()`에서 순차 sentiment 분류 + reply 생성을 `asyncio.gather()`로 병렬 처리

### 8. `review_id=0` 고아 데이터 해결
- **파일**: `backend/app/routers/agent.py`, `backend/app/models.py`
- **수정**: Reply 모델의 `review_id`를 `nullable=True`로 변경, `review_id or 0`을 `review_id if review_id else None`으로 변경

### 9. `keywords` JSON 문자열 파싱
- **파일**: `backend/app/schemas.py`
- **수정**: `ToneProfileOut`에 `field_validator`를 추가하여 `keywords`(JSON string -> List[str])와 `sample_replies`(JSON string -> List[str]) 자동 변환. 프론트엔드 tone 페이지에서 불필요한 `JSON.parse()` 제거.

### 10. 인터랙티브 div 키보드 접근성
- **파일**: `frontend/src/components/FABMiniPanel.tsx`, `frontend/src/app/dashboard/history/page.tsx`
- **수정**: 클릭 가능한 div에 `role="button"`, `tabIndex={0}`, `onKeyDown(Enter/Space)` 추가

### 11. StarRating ARIA
- **파일**: `frontend/src/components/StarRating.tsx`
- **수정**: `aria-label={rating out of 5 stars}`, `role="img"` 추가

### 12. `_build_system_prompt`에 persona 누락
- **파일**: `backend/app/routers/agent.py`
- **수정**: `create_tone_profile()`에서 `brand_persona.json` 파일을 로드하여 `_build_system_prompt()`에 `persona` 파라미터 전달

### 13. API 타임아웃 추가
- **파일**: `frontend/src/lib/api.ts`
- **수정**: `AbortController` + 30초 타임아웃 추가, 타임아웃 시 한국어 에러 메시지 반환

---

## MEDIUM (6건)

### 14. 히스토리 페이지네이션
- **파일**: `backend/app/routers/agent.py`
- **수정**: `get_history()`에 `page`, `page_size` Query 파라미터 추가, 응답에 `total`, `page`, `page_size` 포함

### 15. 인터페이스 중복 -> types.ts 통합
- **생성**: `frontend/src/lib/types.ts`
- **수정**: `Review`, `Reply`, `Product`, `ProductSummary`, `HistoryItem`, `HistoryDetail`, `GeneratedReply` 인터페이스를 공통 파일로 통합. 각 페이지에서 import하여 사용.

### 16. 로딩 스피너 -> Spinner 컴포넌트
- **생성**: `frontend/src/components/Spinner.tsx`
- **수정**: 중복된 스피너 패턴을 공통 컴포넌트로 교체 (history, shop, product detail 페이지)

### 17. `next/image` 사용
- **파일**: `frontend/src/components/ProductCard.tsx`, `frontend/src/app/shop/products/[id]/page.tsx`
- **수정**: `<img>` 태그를 `ProductImage` 컴포넌트 내부의 `next/image` `Image` 컴포넌트로 교체

### 18. 이미지 에러 핸들러 공통화
- **생성**: `frontend/src/components/ProductImage.tsx`
- **수정**: `ProductCard`와 상품 상세 페이지에서 공통 `ProductImage` 컴포넌트 사용 (CRITICAL #4 수정과 연계)

### 19. 이중 db.commit() 수정
- **파일**: `backend/app/routers/agent.py`
- **비고**: reply 저장 후 history 추가 시 여전히 2회 commit이 존재하지만, 이는 reply.id가 필요하기 때문에 의도적으로 유지. flush로 변경하면 트랜잭션 범위가 달라져 동작이 변할 수 있어 현행 유지.

---

## LOW (3건)

### 20. `datetime.utcnow` -> `datetime.now(timezone.utc)`
- **파일**: `backend/app/models.py`, `backend/app/routers/shop.py`, `backend/app/routers/agent.py`
- **수정**: deprecated된 `datetime.utcnow()`를 `datetime.now(timezone.utc)`로 전면 교체

### 21. 미사용 import 제거
- **파일**: `backend/app/routers/agent.py` (HistoryDetailOut 제거), `backend/app/routers/shop.py` (List 제거)

### 22. API 함수 반환 타입 추가
- **파일**: `frontend/src/lib/api.ts`
- **수정**: 모든 API 함수에 명시적 반환 타입(Promise<T>) 추가, `fetchAPI`에 제네릭 타입 파라미터 추가

---

## 빌드 검증
- `next build` 성공 확인 (에러 0건, 기존 useEffect dependency 경고만 존재)
